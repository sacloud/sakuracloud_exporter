# Contributor Requirements

All commits in this repository must comply with the Developer Certificate of Origin
(DCO). Create commits with `git commit --signoff` so that each commit includes a
valid `Signed-off-by` trailer.
